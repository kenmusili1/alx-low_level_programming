#include <stdio.h>
/**
 * master - A program that prints a line using the printf function
 * Return: 0 (Success)
 */


int master(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n")
		Return(0);
}
