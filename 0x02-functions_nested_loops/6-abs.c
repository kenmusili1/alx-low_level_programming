#include "main.h"
/**
  *_abs - Computes tha absolute value
  *of an integer
  *@n: input  num as integer
  *
  *Return: absolute value of n
  */
int _abs(int n)
{
if (n < 0)
return (-n);
else
return (n);
}

